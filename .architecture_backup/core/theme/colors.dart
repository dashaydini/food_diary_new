import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ============================================================
  // LUXURY PALETTE
  // ============================================================

  // Backgrounds
  static const background = Color(0xFF090909);
  static const surface = Color(0xFF12100F);
  static const surfaceTop = Color(0xFF181412);
  static const surfaceSoft = Color(0xFF1D1917);

  // Rose Gold
  static const primary = Color(0xFFB8897B);
  static const primaryLight = Color(0xFFE1A080);
  static const primaryDark = Color(0xFF76544C);

  // Champagne
  static const secondary = Color(0xFFE7D6B5);
  static const champagne = Color(0xFFE7D6B5);
  static const champagneSoft = Color(0xFFCDBB9A);

  // Typography
  static const text = Color(0xFFF1E5D8);
  static const subtitle = Color(0xFFB9A99C);
  static const muted = Color(0xFF81766F);

  // Status
  static const success = Color(0xFF9CAF88);
  static const warning = Color(0xFFD0AD72);
  static const error = Color(0xFFC97979);

  // Rating
  static const star = Color(0xFFE7D6B5);

  // Lines
  static const divider = Color(0xFF4B3530);
  static const border = Color(0xFF704B3E);

  // Overlay
  static const shadow = Color(0x66000000);
}
