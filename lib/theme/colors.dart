import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Luxury dark palette
  static const background = Color(0xFF0D1118);
  static const surface = Color(0xFF11161F);
  static const card = Color(0xFF121821);
  static const cardBg = Color(0xFF121821);

  // Thin elegant borders
  static const cardBorder = Color(0xFF293241);
  static const line = Color(0xFF293241);

  // Champagne / gold
  static const brass = Color(0xFFE6C98F);
  static const champagne = Color(0xFFE6C98F);
  static const gold = Color(0xFFE6C98F);

  // Typography
  static const ink = Color(0xFFF5F1EA);
  static const white = Color(0xFFF5F1EA);
  static const textPrimary = Color(0xFFF5F1EA);
  static const textSecondary = Color(0xFFB6B8BE);
  static const muted = Color(0xFF9DA3AD);

  // Inputs
  static const inputBg = Color(0xFF191F29);

  // Supporting
  static const danger = Color(0xFFE57373);
  static const success = Color(0xFF9BC7A5);
}
